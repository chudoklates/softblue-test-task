/**
 *
 * @param {import('gatsby').PageProps<any, import('../types').HomeContext>} props
 * @returns
 */
const Home = ({ pageContext }) => {
  const { comments, experience, services } = pageContext;

  return null;
};

export default Home;
